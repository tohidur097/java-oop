public class Person {               // 👈 Person নামে একটা ক্লাস তৈরি করা হয়েছে
    private String name;            // 👈 name নামের private (ব্যক্তিগত) ভেরিয়েবল
    private int age;                // 👈 age নামের private integer ভেরিয়েবল
    
    public void setName(String name) {   // 👈 setName() method – name সেট করার জন্য
        this.name = name;                // 👈 this.name = ক্লাসের ভেরিয়েবল, name = method-এর প্যারামিটার
    }
    
    public String getName() {            // 👈 getName() method – name রিটার্ন করার জন্য
        return name;                     // 👈 name ভেরিয়েবলের মান ফেরত দিচ্ছে
    }
    
    public void setAge(int age) {        // 👈 setAge() method – age সেট করার জন্য
        this.age = age;                  // 👈 ক্লাসের age ভেরিয়েবলকে প্যারামিটারের মান দিচ্ছে
    }
    
    public int getAge() {                // 👈 getAge() method – age রিটার্ন করার জন্য
        return age;                      // 👈 age ভেরিয়েবলের মান ফেরত দিচ্ছে
    }

    public static void main(String[] args) {  // 👈 প্রোগ্রামের main method (entry point)
        Person p1 = new Person();             // 👈 নতুন Person অবজেক্ট তৈরি (p1)
        
        p1.setName("Tohidur");                // 👈 setName() ডেকে name সেট করা হলো
        p1.setAge(22);                        // 👈 setAge() ডেকে age সেট করা হলো
        
        System.out.println(p1.getName());     // 👈 getName() ডেকে name প্রিন্ট করা
        System.out.println(p1.getAge());      // 👈 getAge() ডেকে age প্রিন্ট করা
    }    
}
